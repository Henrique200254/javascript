let numeros = [1, 2, 3, 4, 5, 6]

let dobro = numeros.map((numero) => {
    return numero * 2
})
console.log(dobro)

/*
let dobro = numeros.map((numero, indice, arrayCompleto) => {
    console.log(arrayCompleto)
})

*/
